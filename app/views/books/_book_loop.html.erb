<table>
  <thead>
    <tr>
      <th></th>
      <th id="author">Author</th>
      <th>Title</th>
      <th id="publisher">Publisher</th>
      <th id="year">Year</th>
      <th>Genre</th>
      <th>Note</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <div class="modal fade" id="myModal_<%= book.id %>" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
          <div class="modal-dialog" role="document">
            <div class="modal-content">
              <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">×</span></button>
              </div>
              <div class="modal-body">
                <% if book.image.url.nil? == false %>
                  <p><%= image_tag book.image.url, class: "book-pics" %></p>
                <% end %>
                <h3><%= link_to book.title, book %></h3>
                <p><%= book.author %></p>
                <p><%= book.publisher %> <%= book.publication_date %></p>
                <p><%= link_to genre_display_on_index_page(book), generical_path(genre_id: book.genre_id) %> <%= book.format %></p>
                
                <p><strong>Located:</strong><%= book.location %></p>
                <p><strong>Note:</strong></p>
                <p><%= book.note %></p>
                <p><%= number_to_currency book.price %></p>
              </div>
              <div class="modal-footer">
                <p>
          <!-- empty for now -->
                </p>
              </div>
            </div>
          </div>
        </div>
        <td><a href="" data-toggle="modal" data-target="#myModal_<%= book.id %>" > More Info</a></td>
        <td id="author"><%= book.author %></td>
        <td><%= book.title %></td>
        <td id="publisher"><%= book.publisher %></td>
        <td id="year"><%= book.publication_date %></td>
        <td><%= genre_display_on_index_page(book) %>
        <td><%= book.note %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Delete', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Book', new_book_path %>
